#include<stdio.h>
#include<conio.h>
main()
{
	int i,j,len,rails,count,a[100][1000];
	char str[1000];
	printf("Enter a secret message");
	gets(str);
	len=strlen(str);
	printf("Enter a number of rails");
	scanf("%d",&rails);
	gets(
}
